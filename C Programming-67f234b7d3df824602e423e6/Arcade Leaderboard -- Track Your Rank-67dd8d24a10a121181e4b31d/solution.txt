[{"code":"void trackPlayerRanks(int ranked[], int n, int player[], int m, int result[]) {\r\n    int unique[200000];\r\n    int u = 0;\r\n\r\n    // Step 1: Create dense leaderboard (remove duplicates)\r\n    unique[u++] = ranked[0];\r\n    for (int i = 1; i < n; i++) {\r\n        if (ranked[i] != ranked[i - 1]) {\r\n            unique[u++] = ranked[i];\r\n        }\r\n    }\r\n\r\n    // Step 2: Traverse from lowest rank upwards\r\n    int i = u - 1;  // pointer on leaderboard (lowest score)\r\n\r\n    for (int j = 0; j < m; j++) {\r\n        while (i >= 0 && player[j] >= unique[i]) {\r\n            i--;\r\n        }\r\n        result[j] = i + 2;  // dense rank\r\n    }\r\n}\r\n","language":"c","time":"2026-01-10T16:12:35.507Z","isCorrect":true}]